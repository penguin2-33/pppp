# 配电房场景搭建——沉淀总结文档

> 版本：v1
> 更新日期：2026-08-22
> 定位：本轮场景搭建工作的完整沉淀，包含「改了什么、为什么改、踩了什么坑、下一步怎么走」

---

## 一、本轮做了什么（一句话）

从 M1 的"能跑通但场景不合理"状态，迭代修复 + 重构到「目标真实安装在配电柜面板上、仪表有刻度指针、指示灯 3 绿 1 红、机器人不再坠地」的可答辩状态。

---

## 二、修复的 5 个关键 Bug

| # | 问题 | 现象 | 根因 | 修复 |
|---|---|---|---|---|
| 1 | 相机朝向反了 | 抓帧看到机器人自己 + 地板，看不到设备 | v1 设 `[0, -π/2, 0]` 实际让相机看 -X 后方 | v2 改为 `[0, +π/2, 0]`（绕 Y +90°），真正看前 |
| 2 | 抓帧全黑 | `getVisionSensorCharImage` 返回正常 shape 但 mean=0 | 显式处理模式下没先触发渲染 | `grab_image` 前先调 `sim.handleVisionSensor(h)` |
| 3 | 仪表子对象坐标错位 | 刻度环/指针漂到表盘外或嵌进表盘 | 父对象（dial）绕 X 旋转后，子对象本地坐标系轴含义改变 | 用 dial 本地坐标重新描述刻度/指针位置（见第三节坐标系规则） |
| 4 | 编号牌漂到柜顶 | idplate 挂在 cabinet 子对象层级错误 | 层级应是 cab→panel→idplate→数字，而非 cab→idplate | 修正父子层级 |
| 5 | 小车驶出地板坠落 | 机器人走到 x>2.5 掉下去 | CoppeliaSim 默认 Floor 仅 5×5m | 自建 12m×4m 大地板，清场时删除默认 Floor |

---

## 三、关键坐标系规则（最重要，后续 M2~M7 都要用）

CoppeliaSim 是 **Z-up 右手坐标系**（X 向右、Y 进深、Z 向上）。

**欧拉角顺序**（`setObjectOrientation(obj, parent, [α, β, γ])`）：
- 旋转矩阵 R = Rx(α) · Ry(β) · Rz(γ)
- **第一个参数 α 绕 X 轴、第二个 β 绕 Y、第三个 γ 绕 Z**
- 注意：`getObjectOrientation` 返回顺序与直觉相反，**以实际效果为准，别硬记**

**dial 绕 X +90° 后的本地坐标映射**（仪表盘案例）：
| dial 本地轴 | 旋转后世界方向 |
|---|---|
| +X | 世界 +X（不变） |
| +Y | 世界 +Z（向上） |
| +Z | 世界 -Y（朝通道，即朝机器人） |
| -Z | 世界 +Y（朝背墙） |

**父对象旋转后，子对象用「父对象本地坐标」描述**，不要用世界坐标。定位新方向时，用 `setObjectPosition(dummy, parent, [...])` + `getObjectPosition(dummy, -1)` 反查实测。

---

## 四、场景布局设计（最终版）

### 4.1 布局原则
- 设备沿 **X 轴** 一排（Y=0 靠墙），机器人在 **Y=-1** 通道行进，+X 为前进方向
- 仪表/指示灯/开关/按钮**都是配电柜前面板的安装件**（柜体子对象），柜体才是主体
- 警示标识单独挂背墙

### 4.2 三个配电柜分工

| 柜子 | 位置 (x,y,z) | 面板安装内容 | 对应判读任务 |
|---|---|---|---|
| cabinet_01（进线柜） | (0, 0, 0.8) | 编号牌 + 旋钮开关 + 绿启动/红停止按钮 | 开关位置判读 |
| cabinet_02（仪表柜） | (1.6, 0, 0.8) | 编号牌 + 指针仪表 | 仪表读数判读/超限异常 |
| cabinet_03（指示柜） | (3.2, 0, 0.8) | 编号牌 + 3 绿灯 + 1 红灯 | 指示灯颜色/亮灭判读、红灯告警异常 |

### 4.3 关键尺寸
- 柜体：0.8 × 0.4 × 1.6（宽×深×高）
- 仪表盘：直径 0.18，12 个弧形刻度块（270° 量程）+ 红色指针 + 黑色轴帽
- 指示灯：直径 0.08 球体，间距 0.20m，一排 4 个
- 地板：12 × 4m，覆盖 X∈[-3,9] Y∈[-2.5,1.5]

### 4.4 命名约定（config.py 已同步）
- 柜子：`/cabinet_01` ~ `/cabinet_03`
- 仪表：`/cabinet_02/meter_01`（柜子子对象，完整路径）
- 灯：`/cabinet_03/lamp_green_1`~`_3`、`/cabinet_03/lamp_red`
- 按钮：`/cabinet_01/btn_start`、`/cabinet_01/btn_stop`
- 开关：`/cabinet_01/switch_01`
- 标识：`/sign_01`（挂背墙）

---

## 五、踩坑记录（答辩「配置调试与问题解决」素材）

完整踩坑清单已沉淀到《交接文档》第 7 节，共 14 条。本轮新增的 6 条重点：

| # | 坑 | 教训 |
|---|---|---|
| 9 | 抓帧全黑 | 显式处理模式下，抓帧前必须先 `handleVisionSensor` 触发一帧 |
| 10 | 相机朝向反了 | `[0,-π/2,0]` 看后方，`[0,+π/2,0]` 才看前 |
| 11 | 旋转后子对象坐标反直觉 | 父对象旋转后，子对象用父本地坐标描述，用探针实测定位 |
| 12 | cv2.imwrite 中文路径静默失败 | 落盘先写 tempfile 再 shutil.move |
| 13 | PowerShell 中文路径乱码 | 用 `cmd /c "chcp 65001 >nul & ..."` 绕过 |
| 14 | 默认地板太小坠落 | 自建大地板替代默认 5×5m Floor |

---

## 六、关于「找现成模型」的结论

用户提出用现成模型替代自建，经过多渠道调研：

| 渠道 | 结果 |
|---|---|
| CoppeliaSim 自带模型库 | ❌ 无配电柜/仪表/指示灯模型 |
| GitHub 开源仓库 | ❌ 只有代码项目，无可导入的配电柜模型资产 |
| Poly Haven（CC0 免注册） | ❌ 搜索 0 结果 |
| GrabCAD / TraceParts / 爱给网 / 3D溜溜 | ⚠️ 需注册登录，且多为 STEP/SolidWorks 格式 |

**结论**：免费直链可导入 CoppeliaSim 的配电柜模型基本不可得。当前自建几何体方案已能达到「真实配电柜」的视觉效果（弧形刻度仪表 + 3绿1红指示灯 + 按钮开关）。

**备选方案（未执行）**：如后续需更精细模型，可登录 GrabCAD 下载 `.stl`（最保险的导入格式），用 `sim.importShape(0, path, 176, 0, scale)` 导入（options=176 = 保留纹理 16 + 对齐包围盒 32 + 忽略上向量 128）。

---

## 七、当前状态与下一步

### 已完成
- ✅ 场景程序化生成 v2（`build_scene.py` 一键可复现）
- ✅ 相机朝向 + 抓帧修复
- ✅ 目标细节补全（编号牌/仪表刻度指针/警示标识/开关指示线/按钮）
- ✅ 布局重构：仪表/灯/开关真正装在柜体面板上
- ✅ 地板扩大，机器人不坠地
- ✅ 仪表盘弧形刻度 + 红色指针 + 轴帽（真实指针表效果）

### 待办（M2~M7，超出场景搭建范畴）
- M2 多点位巡检 + 停靠对准（机器人到 waypoint 后转向 +Y 看设备）
- M3 YOLO 识别 + OCR 编号
- M4 状态判读（指示灯/开关/仪表）
- M5 异常提示（仪表超限/红灯亮起）
- M6 障碍避让
- M7 完整结果输出

### 可能还需打磨的场景细节（可选）
- 柜门把手、门缝、散热格栅、柜顶警示条、底部线缆槽
- 仪表盘加数字刻度值（当前只有刻度线，无数字）

---

## 八、操作速查

```bash
cd D:\具身智能\inspection

python build_scene.py      # 重建场景（幂等，自动清场）
python inspect_scene.py    # 转储对象树核对
python main.py             # 跑 M1 主流程
python reset_state.py      # 停仿真 + 机器人复位
python view_scene.py       # GUI 相机对准场景
```

**遇到 `getObject('/P3DX')` 报「object does not exist」**：说明当前场景被清空了（Edu 版注册窗/多次 stopSimulation 会触发），直接重跑 `build_scene.py`。

---

*本文档与《交接文档.md》《技术方案文档.md》配套，前者管「场景搭建细节」，本文管「本轮改动总结」。*
