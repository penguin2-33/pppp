# 配电房场景搭建——沉淀总结文档

> 版本：v4
> 更新日期：2026-08-22
> 定位：场景搭建工作的完整沉淀，包含「改了什么、为什么改、踩了什么坑、下一步怎么走」

---

## 一、本轮做了什么（一句话）

从 M1 的"能跑通但场景不合理"状态，迭代到 v4：「**真实配电房房间**（三面墙 + 设备靠墙）+ 真实灯光指示灯 + 变压器/灭火器/标识阵列 + 异常与状态判读对象齐全」的可答辩状态。

---

## 二、版本演进（v1 → v4）

| 版本 | 关键变化 |
|---|---|
| v1 | 打通 API：P3-DX + Hokuyo + 相机 + 接近传感器 + 平铺目标（柜子/仪表/灯/标识/开关） |
| v2 | 目标装上柜体面板（柜子子对象）、仪表加刻度指针、相机朝向修正、抓帧修复、大地板 |
| v3 | 异常状态（仪表超限 +150°、红灯告警）+ 状态判读多状态（绿灯灭、开关 OFF）+ 通道标线 + 柜体细节 |
| v4 | **房间结构**（三面墙、地面 12×6m、设备靠墙）+ **真实灯光指示灯** + 变压器/母线桥架/灭火器 + 5 种安全标识 + 地面警示带/绝缘垫 + 柜门观察窗 |

---

## 三、修复的关键 Bug

| # | 问题 | 现象 | 根因 | 修复 |
|---|---|---|---|---|
| 1 | 相机朝向反了 | 抓帧看到机器人自己 + 地板 | v1 设 `[0, -π/2, 0]` 实际看 -X 后方 | v2 改 `[0, +π/2, 0]`（绕 Y +90°）看前 |
| 2 | 抓帧全黑 | `getVisionSensorCharImage` 返回正常但 mean=0 | 显式处理模式下没先触发渲染 | 抓帧前先 `sim.handleVisionSensor(h)` |
| 3 | 仪表子对象坐标错位 | 刻度/指针漂到表盘外 | 父对象 dial 绕 X 旋转后本地轴含义改变 | 用 dial 本地坐标描述子对象 |
| 4 | 编号牌漂到柜顶 | idplate 层级错误 | 层级应是 cab→panel→idplate，而非 cab→idplate | 修正父子层级 |
| 5 | 机器人驶出地板坠落 | 走到 x>2.5 掉下去 | 默认 Floor 仅 5×5m | 自建大地板（v4 为 12×6m） |
| 6 | 指针绕自身中心旋转偏离轴心 | 指针拨角度后根部偏离表盘中心 | 旋转 pivot 是针自身中心而非表盘中心 | 用 **dummy 枢轴**挂表盘中心，旋转枢轴 |
| 7 | 真实光源污染整个房间 | 加了点光源后整房发绿 | 复制的默认点光源是"房间照明级"全向光 | 强度降到 6% + 灯球加 emission 自发光 |

---

## 四、关键坐标系规则（最重要，后续 M2~M7 都要用）

CoppeliaSim 是 **Z-up 右手坐标系**（X 向右、Y 进深、Z 向上）。

**欧拉角顺序**（`setObjectOrientation(obj, parent, [α, β, γ])`）：
- 旋转矩阵 R = Rx(α) · Ry(β) · Rz(γ)
- **第一个参数 α 绕 X、第二个 β 绕 Y、第三个 γ 绕 Z**
- `getObjectOrientation` 返回顺序与直觉相反，**以实际效果为准，别硬记**

**dial 绕 X +90° 后的本地坐标映射**（仪表盘案例）：
| dial 本地轴 | 旋转后世界方向 |
|---|---|
| +X | 世界 +X（不变） |
| +Y | 世界 +Z（向上） |
| +Z | 世界 -Y（朝通道，即朝机器人） |

**父对象旋转后，子对象用「父对象本地坐标」描述**。定位新方向时，用 `setObjectPosition(dummy, parent, [...])` + `getObjectPosition(dummy, -1)` 反查实测。

---

## 五、v4 场景布局设计

### 5.1 房间与通道

- 房间：地面 **12m × 6m**（X∈[-4,8]，Y∈[-3,3]），三面墙（后墙 y=2.9 + 左右墙 x=±3.95/7.95，高 2.8m），**正面敞开**便于展示
- 设备靠后墙（柜体中心 **y=2.6**），机器人在 **y=1.0** 通道行进，+X 为前进方向
- 巡检点位 y=1.0，分别正对 3 个柜子（x=0 / 1.6 / 3.2）

### 5.2 三个配电柜分工

| 柜子 | 位置 (x,y,z) | 面板安装内容 | 对应判读任务 |
|---|---|---|---|
| cabinet_01（进线柜） | (0, 2.6, 0.8) | 编号牌 + 绿启动/红停止按钮 + 旋钮开关(OFF) + **绿灯·电源指示(亮)** + 观察窗 | 开关位置判读 |
| cabinet_02（仪表柜） | (1.6, 2.6, 0.8) | 编号牌 + 指针仪表(超限+150°) + **绿灯·工作指示(亮)** | 仪表读数判读/超限异常 |
| cabinet_03（指示柜） | (3.2, 2.6, 0.8) | 编号牌 + **绿灯·回路(灭)** + **红灯·告警(亮)** | 指示灯亮灭判读、红灯告警异常 |

### 5.3 典型设备与标识

- 变压器（靠后墙 x=5.3，带散热鳍片 + 黄色警示标签）
- 母线桥架（天花板下金属槽）
- 灭火器 ×2（红色罐体，通道远侧）
- 安全标识阵列 ×5：黄色警示 / 高压危险 / 禁止烟火 / 必须接地 / 安全出口

### 5.4 地面细节

- 黄黑警示带（设备前地面，交替黄黑段）
- 绝缘垫（绿色橡胶，设备前操作区）
- 通道边界黄线 ×2

### 5.5 关键尺寸

- 柜体 0.8 × 0.4 × 1.6；仪表盘直径 0.18（13 根刻度，270° 量程）；指示灯直径 0.08
- 变压器 0.9 × 0.6 × 1.4；地板 12 × 6m；墙高 2.8m

### 5.6 命名约定（config.py 已同步）

- 柜子 `/cabinet_01`~`03`；仪表 `/cabinet_02/meter_01`
- 灯 `/cabinet_01/lamp_green_1`、`/cabinet_02/lamp_green_2`、`/cabinet_03/lamp_green_3`、`/cabinet_03/lamp_red`
- 按钮 `/cabinet_01/btn_start|btn_stop`；开关 `/cabinet_01/switch_01`
- 标识 `/sign_01`、`/sign_highvoltage`、`/sign_no_fire`、`/sign_ground`、`/sign_exit`
- 设备 `/transformer`、`/bus_duct`、`/fire_ext_01|02`

---

## 六、真实灯光指示灯方案（答辩亮点）

CoppeliaSim 4.10 **没有直接创建灯光的 API**，指示灯用组合方案：

1. **可见灯罩球体**：亮=亮色 + **emission 自发光**（`setShapeColor(..., colorcomponent_emission, ...)`），让灯球本身"点亮"；
2. **真实点光源**：`sim.copyPasteObjects` 复制默认灯光 `DefaultLightA`（type=13），再用 `sim.setLightParameters(handle, state, None, diffuse, specular)` 设颜色和开关；
3. **强度控制**：默认点光源是房间照明级，直接复制会把房间染成灯色 → **强度降到 6%**，只留灯球附近微弱光晕。

亮/灭 = 真实光源的开关状态（state=1/0），不是"换个颜色假装"。

---

## 七、踩坑记录（答辩「配置调试与问题解决」素材）

完整 14 条已沉淀在《交接文档》第 7 节。v3/v4 新增的 3 条重点：

| # | 坑 | 教训 |
|---|---|---|
| 15 | 指针绕自身中心旋转偏离轴心 | 指针几何中心 ≠ 表盘中心；用 dummy 枢轴挂表盘中心再旋转枢轴 |
| 16 | 真实光源污染整个房间 | 默认点光源是房间照明级全向光；做指示灯要降到 6% 强度 + emission 自发光 |
| 17 | 无 createLight API | 灯光对象无法直接创建；用 `copyPasteObjects` 复制默认灯光 + `setLightParameters` 设参数 |

---

## 八、关于「找现成模型」的结论

| 渠道 | 结果 |
|---|---|
| CoppeliaSim 自带模型库 | ❌ 无配电柜/仪表/指示灯模型 |
| GitHub 开源仓库 | ❌ 只有代码项目，无可导入资产 |
| Poly Haven（CC0） | ❌ 0 结果 |
| GrabCAD / TraceParts / 爱给网 | ⚠️ 需注册，多为 STEP/SolidWorks 格式 |

**结论**：免费直链可导入的配电柜模型基本不可得，自建几何体方案已能达到真实配电房视觉效果。

**备选（未执行）**：GrabCAD 下载 `.stl`，用 `sim.importShape(0, path, 176, 0, scale)` 导入。

---

## 九、当前状态与下一步

### 已完成（场景侧）
- ✅ 场景程序化生成 v4（房间结构 + 设备 + 标识 + 地面 + 真实灯光，一键可复现）
- ✅ 异常状态：仪表超限（+150°）+ 红灯告警
- ✅ 状态判读对象：绿灯3 灭 + 开关 OFF + 按钮
- ✅ 真实灯光指示灯（点光源 + emission 自发光）
- ✅ 三面墙房间 + 地面 12×6m + 设备靠墙 + 正面敞开

### 待办（M2~M7，超出场景搭建范畴）
- M2 多点位巡检 + 停靠对准（机器人到 waypoint 后转向 +Y 看设备）
- M3 YOLO 识别 + OCR 编号
- M4 状态判读（指示灯/开关/仪表）
- M5 异常提示（仪表超限/红灯亮起）
- M6 障碍避让
- M7 完整结果输出

---

## 十、操作速查

```bash
cd D:\具身智能\inspection

python build_scene.py      # 重建场景（幂等，自动清场）
python inspect_scene.py    # 转储对象树核对
python main.py             # 跑 M1 主流程
python reset_state.py      # 停仿真 + 机器人复位
python view_scene.py       # GUI 相机对准场景
```

**遇到 `getObject('/P3DX')` 报「object does not exist」**：场景被清空了（Edu 版注册窗/多次 stopSimulation 触发），重跑 `build_scene.py`。

---

*本文档与《交接文档.md》《技术方案文档.md》配套，前者管「场景搭建细节」，本文管「场景搭建整体总结」。*
