# GitHub 提交总结

> 版本：v1
> 更新日期：2026-08-23
> 定位：本次场景改动与 GitHub 提交的沉淀

---

## 一、本次做了什么（一句话）

修复场景指示灯位置（cabinet_02 灯贴地 + 三柜绿灯高度对齐）→ 重新生成场景 → 提交到 GitHub。

---

## 二、GitHub 仓库信息

| 项 | 值 |
|---|---|
| 仓库地址 | https://github.com/penguin2-33/pppp |
| 远程名 | `origin` |
| 分支 | `master` |
| 访问方式 | HTTPS |

---

## 三、提交历史（4 个提交）

| 提交 | 内容 |
|---|---|
| `793f96b` | 初始提交（场景 v3 + M1 闭环 + 文档） |
| `34fea2f` | 场景升级 v4（房间 + 真实灯光 + 设备） |
| `e66a0e6` | 修正指示灯位置（灯贴地 + 三柜对齐） |
| `b970f17` | 同步最新场景文件（指示灯修复后重新生成） |

---

## 四、本次场景改动（灯位置）

| 灯 | 所属柜 | 改动 | 说明 |
|---|---|---|---|
| `lamp_green_1` | cabinet_01 | `lz 0.15 → 0.45` | 与另外两柜对齐到 z=1.25 |
| `lamp_green_2` | cabinet_02 | `lz -0.45 → 0.45` | 修复贴地（原在 z=0.35，被柜体/仪表挡住） |

**关键教训**：`build_scene.py` 生成的场景保存到项目外的 `D:\inspection_scene.ttt`，而 git 跟踪的是项目内的 `配电房巡检场景.ttt`。每次 `build_scene.py` 后需手动 `copy` 同步，否则 git 里的场景文件是旧版。本次就是漏了这一下，导致"场景文件没提交"。

---

## 五、日常提交流程（速查）

```bash
cd D:\具身智能\inspection
python build_scene.py                 # 重新生成场景（含修改）

# 同步最新场景到项目内（容易漏的一步！）
copy /Y "D:\inspection_scene.ttt" "D:\具身智能\配电房巡检场景.ttt"

cd D:\具身智能
git add -A
git commit -m "说明"
git push
```

---

## 六、遗留事项（待决策）

**场景文件同步易漏**：`build_scene.py` 保存到项目外，是否考虑改代码让它直接保存到项目内 `配电房巡检场景.ttt`，从根上避免漏同步。

---

*本文档与《交接文档.md》《场景搭建总结.md》配套，专门沉淀 Git 提交与场景改动经验。*
