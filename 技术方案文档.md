# 工业配电房智能巡检——技术方案文档（冲高分版）

> 赛道：职教赛道（应用与调试）
> 平台：CoppeliaSim（ZMQ Remote API）
> 控制：Python
> 目标：冲高分——规则保底 + YOLO 微调 + OCR + 仪表读数全覆盖

---

## 一、总体定位与冲分目标

**策略一句话**：用 OpenCV 规则方法保证基础功能"稳、零训练、不翻车"，用 YOLO/OCR/仪表读数把"目标识别""异常提示"等项的分拉满，最终实现**连续、完整、可核验**的巡检闭环。

**冲分原则**：
- 评分项全做、全做满，不留"不计分"的硬伤。
- 场景规格**高于最低要求**：3 点位、6 类目标、2 类状态判读、2 类异常、2 个障碍物。
- 识别全部基于实时仿真数据，守住真实性红线。

---

## 二、技术选型总览

| 模块 | 技术选型 | 冲高分理由 |
|---|---|---|
| 仿真平台 | CoppeliaSim | 官方认可、Vision Sensor/LiDAR 齐全 |
| 机器人 | Pioneer P3-DX 差速底座 + fast Hokuyo LiDAR + 云台 Vision Sensor + 接近传感器 | 差速稳定、三层感知可讲集成 |
| 通信 | ZMQ Remote API（Python） | 稳定、支持多客户端、易集成 |
| 主力目标检测 | Ultralytics YOLOv8 微调 | 一个模型覆盖 6 类目标，拿额外识别分 |
| 编号/标识识别 | PaddleOCR | 识别柜体编号文字 |
| 指示灯状态 | OpenCV 颜色/亮度规则 | 零训练、最稳 |
| 开关/旋钮状态 | OpenCV 形状/颜色/角度 | 零训练 |
| 指针仪表读数 | OpenCV 几何法（霍夫圆+直线→角度→读数） | 服务异常判读 |
| 障碍感知 | fast Hokuyo LiDAR + 接近传感器 | 主感知绕行 + 近距离急停 |
| 结果输出 | 终端 + JSON + CSV + 现场截图 | 完整可核验、有辅助记录加分 |

---

## 三、系统架构

```
CoppeliaSim 仿真场景（配电房）
   ├─ 机器人（轮式 + 云台相机 + LiDAR + 接近传感器）
   ├─ 3 个巡检点位
   ├─ 6 类目标（电柜、仪表、指示灯、警示标识、开关、障碍物）
   └─ 2 个障碍物（1 静态 + 1 动态临时出现）
            │  ZMQ Remote API
            ▼
Python 控制端（inspection/ 包）
   main.py（主流程编排）
   ├─ config.py        任务配置（点位/顺序/参数/输出）
   ├─ env_check.py     仿真环境检查
   ├─ navigation.py    点位导航与稳定停靠
   ├─ perception.py    Vision Sensor 抓帧 + YOLO + OCR
   ├─ state_judge.py   状态判读（规则）
   ├─ anomaly.py       异常检测（仪表超限/红灯亮起）
   ├─ obstacle.py      障碍感知与避让
   └─ report.py        结果记录与输出
```

---

## 四、仿真场景设计（高于最低要求）

| 类别 | 设计规格 | 赛题最低要求 |
|---|---|---|
| 巡检点位 | **3 个**（点位1=进线柜区、点位2=仪表/指示灯区、点位3=开关/警示区） | ≥2 |
| 典型目标 | **6 类**：配电柜编号、指针仪表、指示灯、警示标识、开关/旋钮、通道障碍物 | ≥2 |
| 状态判读 | **2~3 类**：指示灯颜色/亮灭、开关位置、通道畅通/受阻 | ≥1 |
| 异常状态 | **2 类**：仪表读数超限、红色告警灯异常亮起 | ≥1 |
| 障碍物 | **2 个**：静态物料箱 + 动态临时出现的检修工具车 | ≥1 |

**场景布局原则**：设备排列规则、巡检对象集中、通道边界明确、障碍物在行进途中触发出现（体现动态感知）。

---

## 五、模块设计

### 1. config.py —— 任务配置
- 巡检点位坐标/朝向（预设路径点）
- 运行顺序、每点位需识别的目标类型
- 状态判读规则参数（颜色阈值、开关位置阈值）
- 异常规则参数（仪表正常范围、红灯亮灭判定）
- 结果输出路径/格式（JSON + CSV + 截图目录）

### 2. env_check.py —— 环境检查（对应评分项2）
- 场景资源加载确认（关键对象 handle 是否存在）
- 机器人模型状态检查
- Vision Sensor / LiDAR / 接近传感器句柄检查
- ZMQ 通信链路检查
- 输出检查报告（终端打印 + 记录）

### 3. navigation.py —— 点位导航与停靠（对应评分项4）
- 预设路径点 + 纯追踪/PID 驱动轮式机器人
- 到达判定（位置 + 朝向误差阈值）
- 停靠微调（前后微调、转向对准目标）
- 记录每个点位的到达耗时

### 4. perception.py —— 视觉识别（对应评分项5）
- `sim.getVisionSensorCharImage` 实时抓帧 → numpy
- YOLOv8 检测 6 类目标（电柜、仪表、指示灯、警示标识、开关、障碍物）
- 对"编号牌"区域调用 PaddleOCR 读编号
- 输出：目标类别 + 位置框 + 置信度 + 编号文字

### 5. state_judge.py —— 状态判读（对应评分项6）
- 指示灯：YOLO 定位 → ROI 颜色阈值 → 输出颜色 + 亮灭
- 开关/旋钮：YOLO 定位 → 轮廓/颜色/角度 → 输出位置（ON/OFF 或档位）
- 通道：结合 LiDAR → 输出畅通/受阻

### 6. anomaly.py —— 异常检测（对应评分项7）
- 异常1：指针仪表读数 > 正常范围 → 记录点位/对象/类型/读数
- 异常2：红色告警灯由熄灭变为亮起 → 记录点位/对象/类型
- 输出：清晰终端告警 + 保存现场截图 + 检测时间

### 7. obstacle.py —— 障碍感知与处置（对应评分项8）
- LiDAR 主感知：前方障碍距离 → 减速
- 判定通道宽度 → 绕行或停车等待
- 接近传感器：近距离急停保护
- 记录避障过程（无碰撞、不需人工）

### 8. report.py —— 结果输出（对应评分项9）
- 终端实时打印
- JSON 结构化结果文件
- CSV 巡检表格（一行一点位）
- 保存每点位现场截图（异常时额外保存）
- 汇总：任务完成状态、到达点位、识别目标、状态判读、异常提示、耗时、碰撞次数、人工接管次数

---

## 六、目标识别方案（冲高分：6 类全覆盖）

| 目标 | 识别方式 | 技术 |
|---|---|---|
| 配电柜编号/设备标识 | YOLO 定位 + OCR 读文字 | YOLOv8 + PaddleOCR |
| 指针仪表 | YOLO 定位 + 几何法读指针 | YOLOv8 + OpenCV |
| 指示灯 | YOLO 定位 + 颜色/亮度判断 | YOLOv8 + OpenCV |
| 安全警示标识 | YOLO 定位（黄黑/红框特征） | YOLOv8 |
| 开关/旋钮 | YOLO 定位 + 位置判断 | YOLOv8 + OpenCV |
| 通道障碍物 | YOLO 定位 + LiDAR 距离 | YOLOv8 + LiDAR |

**YOLO 微调流程**：从 CoppeliaSim 多角度截图 → 标注 6 类 → 训练 YOLOv8n/s → 导出 best.pt → 集成到 perception.py。

---

## 七、评分逐项对应策略（冲满分要点）

| # | 评分项 | 冲分动作 |
|---|---|---|
| 1 | 场景与任务准备 | 3点位、6类目标、2类异常、2障碍，全部在方案/视频中明确 |
| 2 | 环境检查与规范操作 | env_check.py 输出检查报告，视频里演示检查流程 |
| 3 | 任务配置与点位设置 | config.py 集中配置，视频演示配置过程 |
| 4 | 点位到达与稳定停靠 | 3点位全到达 + 停靠微调 + 无遥控 |
| 5 | 典型目标识别 | YOLO 6 类 + OCR 编号，拿满额外目标 2 分 |
| 6 | 目标状态判读 | 指示灯 + 开关 + 通道 3 类判读，规则参数说明清楚 |
| 7 | 异常提示与记录 | 2 类异常 + 点位/对象/类型 + 时间 + 截图，全拿 |
| 8 | 障碍感知与处置 | LiDAR 绕行 + 接近急停 + 无碰撞 + 继续任务 |
| 9 | 结果输出与稳定性 | JSON+CSV+截图+终端，记录耗时/碰撞/接管 |
| 10 | 完整性与真实性 | 报告/代码/视频/PPT 相互对应，外部来源说明清楚 |

---

## 八、开发里程碑

1. **M1 最小闭环**：场景 + 机器人 + Vision Sensor + ZMQ 抓帧跑通
2. **M2 导航停靠**：3 点位依次到达 + 停靠微调
3. **M3 目标识别**：YOLO 微调 + OCR + 规则判读
4. **M4 状态判读 + 异常提示**：指示灯/开关/通道 + 仪表超限/红灯
5. **M5 障碍避让**：LiDAR 绕行 + 动态障碍物
6. **M6 结果输出**：JSON/CSV/截图完整闭环
7. **M7 打磨**：连续稳定运行 + 演示视频 + 方案报告 + PPT

---

## 九、已确定事项与剩余待确认

**已确定**：
1. **CoppeliaSim 版本**：4.10.0（ZMQ Remote API 内置，全程使用 ZMQ）
2. **机器人方案**：Pioneer P3-DX 差速底座 + fast Hokuyo LiDAR + 云台 Vision Sensor + 前方接近传感器

**剩余待确认**：
1. 是否已有场景/机器人模型，还是从零搭建
2. 团队成员分工（主汇报人 + 代码/场景/报告）

---

*本文档为技术方案沉淀，与《赛题规则文档.md》配套使用，后续开发、报告、答辩均以此为施工蓝图。*
