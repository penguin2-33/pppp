# 工业配电房智能巡检 — 开发交接文档

> 版本：v1（M1 完成）
> 更新日期：2026-08-22
> 状态：场景已建成、M1 最小闭环已跑通，进入 M2~M7 功能开发阶段

---

## 1. 项目概述

- **赛题**：工业配电房智能巡检（职教赛道，机器人仿真竞赛）
- **平台**：CoppeliaSim 4.10.0（Edu 版）
- **控制语言**：Python（ZMQ Remote API）
- **机器人**：Pioneer P3-DX 差速底盘
- **传感器**（三级感知分工）：
  - 视觉 `VisionSensor`（透视相机 640×480）→ 「是什么」（语义识别）
  - 激光 `Hokuyo`（fast URG-04LX）→ 「多远」（精确测距）
  - 接近 `proxFront`（锥形）→ 「最后急停防线」
- **目标**：冲高分，答辩越有技术含量越好

---

## 2. 当前进度状态

| 阶段 | 状态 | 说明 |
|---|---|---|
| 场景程序化生成 v4 | ✅ 完成 | `build_scene.py` v4：房间结构 + 真实灯光 + 变压器/标识阵列 + 异常/状态判读对象 |
| M1 最小闭环 | ✅ 完成 | 连接→启动仿真→19 项环境检查→抓帧（有效画面）→导航到点位1→结果记录 |
| M2 多点位巡检 | ⬜ 未开始 | |
| M3 YOLO 识别 + OCR | ⬜ 未开始 | |
| M4 状态判读 | ⬜ 未开始 | |
| M5 异常提示 | ⬜ 未开始 | |
| M6 障碍避让 | ⬜ 未开始 | |
| M7 完整结果输出 | ⬜ 未开始 | |
| 方案报告 / PPT / 演示视频 | ⬜ 未开始 | |

---

## 3. 环境与运行

### 3.1 环境

- CoppeliaSim：`C:\Program Files\CoppeliaRobotics\CoppeliaSimEdu`
- Python：3.9（`C:\Users\ASUS\AppData\Local\Programs\Python\Python39`）
- 依赖（`inspection/requirements.txt`）：
  - `numpy`、`opencv-python`、`coppeliasim-zmqremoteapi-client`
- ZMQ 客户端已安装于系统 site-packages

### 3.2 一键搭建场景

```bash
cd D:\具身智能\inspection
python build_scene.py
```

- 前提：CoppeliaSim 已打开（任意场景，脚本会自动清场重建）
- 保存路径：`D:\inspection_scene.ttt`（英文路径）
- 项目内已另存：`D:\具身智能\配电房巡检场景.ttt`
- 幂等：可重复运行

### 3.3 运行主流程

```bash
python main.py
```

### 3.4 辅助脚本

| 脚本 | 作用 |
|---|---|
| `inspect_scene.py` | 转储场景对象树（核对命名/位置） |
| `reset_state.py` | 停仿真 + 机器人复位到起点 |
| `view_scene.py` | 停仿真 + 复位 + 默认相机对准场景 |

---

## 4. 文件清单

```
D:\具身智能\
├── 赛题规则文档.md          # 赛题 8 模块 + 红线 + 评分 + 时间线
├── 技术方案文档.md          # 技术选型 + 里程碑 M1~M7
├── 答辩策略与技术亮点.md    # 8 大技术亮点 + 调试故事库 + 答辩节奏
├── 场景搭建指南.md          # 手动搭建指南（已被 build_scene.py 取代）
├── 交接文档.md              # 本文档
├── 配电房巡检场景.ttt       # 场景副本（脚本输出为 D:\inspection_scene.ttt）
└── inspection\
    ├── build_scene.py       # 场景程序化生成（核心）
    ├── config.py            # 命名约定 + 导航/障碍参数 + 输出路径
    ├── robot.py             # 差速驱动 + go_to_point 比例控制导航
    ├── sensors.py           # 视觉/激光/接近传感器封装
    ├── env_check.py         # 场景对象句柄检查
    ├── main.py              # M1 主流程
    ├── inspect_scene.py     # 场景树转储工具
    ├── reset_state.py       # 复位工具
    ├── view_scene.py        # 相机对准工具
    ├── README.md            # 运行说明
    └── requirements.txt     # 依赖
```

---

## 5. 场景结构（v4）

### 5.1 房间与布局

- 房间：地面 **12m × 6m**（X∈[-4,8]，Y∈[-3,3]），**三面墙**（后墙 y=2.9 + 左右墙 x=±3.95/7.95，高 2.8m），**正面敞开**便于展示
- 设备靠后墙（柜体中心 **y=2.6**），机器人在 **y=1.0** 通道行进，+X 为前进方向

| 对象 | 位置 (x, y, z) |
|---|---|
| 机器人起点 | (-2.0, 1.0, 0.2) |
| 点位 1 / 2 / 3 | (0, 1.0) / (1.6, 1.0) / (3.2, 1.0) |
| 配电柜 1 / 2 / 3 | (0, 2.6, 0.8) / (1.6, 2.6, 0.8) / (3.2, 2.6, 0.8) |
| 变压器 | (5.3, 2.6, 0.7) |
| 障碍物 1 / 2 | (2.4, 1.0, 0.25) / (6.8, 0.3, 0.25) |
| 灭火器 | (-2.5, 0, 0.2) / (6.5, 0, 0.2) |

### 5.2 三个配电柜分工（含指示灯分布）

| 柜子 | 面板内容 | 对应任务 |
|---|---|---|
| cabinet_01 进线柜 | 编号牌 + 绿/红按钮 + 旋钮开关(OFF) + **绿灯·电源(亮)** + 观察窗 | 开关位置判读 |
| cabinet_02 仪表柜 | 编号牌 + 指针仪表(超限+150°) + **绿灯·工作(亮)** | 仪表读数/超限异常 |
| cabinet_03 指示柜 | 编号牌 + **绿灯·回路(灭)** + **红灯·告警(亮)** | 灯亮灭判读、红灯异常 |

### 5.3 关键尺寸

- 配电柜 0.8 × 0.4 × 1.6；仪表盘直径 0.18（13 根刻度，270° 量程）；指示灯直径 0.08
- 变压器 0.9 × 0.6 × 1.4；地板 12 × 6m；墙高 2.8m；障碍物 0.4 × 0.3 × 0.5

### 5.4 命名约定（`config.py`，`sim.getObject('/路径')` 依赖 alias）

| 对象 | 路径 |
|---|---|
| 机器人底座 | `/P3DX` |
| 左/右轮关节 | `/P3DX/leftMotor` `/P3DX/rightMotor` |
| 视觉传感器 / 激光 / 接近 | `/P3DX/VisionSensor` `/P3DX/Hokuyo` `/P3DX/proxFront` |
| 点位 | `/waypoint_01` ~ `/waypoint_03` |
| 仪表 | `/cabinet_02/meter_01` |
| 灯 | `/cabinet_01/lamp_green_1`、`/cabinet_02/lamp_green_2`、`/cabinet_03/lamp_green_3`、`/cabinet_03/lamp_red` |
| 按钮/开关 | `/cabinet_01/btn_start|btn_stop`、`/cabinet_01/switch_01` |
| 标识 | `/sign_01`、`/sign_highvoltage`、`/sign_no_fire`、`/sign_ground`、`/sign_exit` |
| 设备 | `/transformer`、`/bus_duct`、`/fire_ext_01|02` |
| 障碍物 | `/obstacle_01`、`/obstacle_02` |

---

## 6. 已验证能力（M1 闭环）

运行 `python main.py` 输出：

1. 连接 CoppeliaSim（ZMQ 23000）
2. 启动仿真
3. 环境检查：19 项对象全部 OK（机器人/传感器/点位/目标/障碍）
4. 视觉抓帧：640×480，**有效画面**（v2 起抓帧前会调 sim.handleVisionSensor 触发一帧），保存 `output/shots/m1_frame_test.png`（能看到 2 个配电柜的顶部 + 编号牌）
5. 自主导航到点位 1：**24~36 步到达**（差速运动学 + 比例控制；步数因起始位姿略有差异）
6. 结果记录：`output/inspection_result.json`

机器人物理：起始 z=0.2 落下后稳定于 **z=0.139**（轮底贴地）。

v2 场景目标细节（已补全）：
- 配电柜前面板 + 浅色编号牌 + 2 个数字方块（视觉化"01""02""03"，便于 M3 OCR 训练/测试）
- 指针仪表：表盘 + 刻度环 + 4 个刻度点（12/3/6/9 点位）+ 旋转指针
- 警示标识：黄色底 + 黑色 4 边框 + 45° 斜杠
- 开关/旋钮：主体 + 朝通道的亮黄色指示线

---

## 7. 关键 API 踩坑记录（答辩「配置调试与问题解决」素材，25 分）

> 这是本次开发最有价值的沉淀，答辩时可作为「调试故事」展开。

| # | 坑 | 现象 | 解决 |
|---|---|---|---|
| 1 | `sim.setShapeColor` 参数个数 | 传 3 参 → 创建第一个柜子即崩，后续全缺 | 正确签名 4 参：`(handle, colorName, colorComponent, rgbData)` |
| 2 | 形状常量混用 | `createPureShape` 用了 `pure_primitive_cuboid(=3)`，实际建出**圆锥** | 用新 API `createPrimitiveShape` + `primitiveshape_*`（cuboid=3/spheroid=4/cylinder=5）；旧 `createPureShape` 用 0/1/2/3 |
| 3 | `removeObjects` 删父不删子 | 旧模型子对象成孤儿残留 → 重名报错 | 用 `getObjectsInTree(scene, handle_all)` 取全部对象，深度优先全部删除 |
| 4 | `getObject('/路径')` 解析失败 | 只设了 name 没设 alias | 需 `sim.setObjectAlias`（合法字符仅 0-9 a-z A-Z _） |
| 5 | 机器人掉穿地板 | 起始 z=0 嵌入地板 → 物理引擎弹穿（坠到 z=-1007） | 起始抬高到 z=0.2，自然落下 |
| 6 | `saveScene` 中文路径乱码 | 保存成 `D:\鍏疯韩鏅鸿兘\...` | 中文 Windows 下非 ASCII 路径被按 GBK 误解 → 改用英文路径 |
| 7 | `getVisionSensorCharImage` 返回值 | 解包 2 值报错 | 实际返回 3 值：`(buffer, resX, resY)` |
| 8 | `getObjectPosition` 传字符串 | 「argument 1 is not a number」 | 需先 `sim.getObject()` 取句柄，再传句柄 |
| 9 | 视觉传感器**抓帧全黑** | `getVisionSensorCharImage` 返回正常 shape，但 mean=0（黑图） | options=3 已含显式处理（`getExplicitHandling=1`），但**仍需在抓帧前调 `sim.handleVisionSensor(h)` 显式触发一帧**——否则缓冲是上一帧或全 0 |
| 10 | 视觉传感器**朝向反了** | v1 设 `[0, -π/2, 0]` 让相机看机器人 -X 后方（看到机器人和地板） | v2 改为 `[0, +π/2, 0]` 真正看前（看到配电柜）；相机默认观察方向是局部 +Z，绕 Y +π/2 把 +Z 转到 +X |
| 11 | 旋转后子对象坐标系反直觉 | dial 绕 X +90° 后，设 `[0,0,+r]` 实际转到 -y 方向（朝通道） | 用 dial 本地坐标记：+X=世界+X，+Y=世界+Z（上），+Z=世界-Y（通道）。**别用世界坐标描述子对象位置**——用 dial 旋转后**本地 +Z 对应世界 -Y** 容易反。**实证办法**：用 setObjectPosition(dummy, parent, ...) + getObjectPosition(dummy, -1) 反查 |
| 12 | cv2.imwrite 中文路径静默失败 | main.py 报"已保存"但实际无文件 | OpenCV 4.x 在 Windows 中文路径下 imwrite 偶尔返回 False 不抛异常；落盘前先写 `tempfile` 再 shutil.move |
| 13 | PowerShell 中文路径参数乱码 | 执行 `cd d:\具身智能` 被 GBK 误解为 `D:\鍏疯韩鏅鸿兘` | 用 `cmd /c "chcp 65001 >nul & ..."` 走 cmd 通道，路径不会被 PowerShell 二次编码 |
| 14 | **默认地板太小，小车驶出坠落** | CoppeliaSim 默认 `Floor` 仅 5×5m（X/Y ∈ [-2.5, 2.5]），机器人沿通道走到 x>2.5 即驶出边界坠落 | 自建大地板取代默认 Floor：`createPrimitiveShape` 一块 12m×4m 平板（X∈[-3,9] Y∈[-2.5,1.5]），顶面置于 z=0；清场时把 `Floor`/`box` 一并删除 |
| 15 | **指针绕自身中心旋转偏离轴心** | 仪表指针拨角度后，指针根部偏离表盘中心 | 指针几何中心 ≠ 表盘中心；用 dummy 枢轴挂在表盘中心，旋转枢轴而非指针 |
| 16 | **真实光源污染整个房间** | 加了点光源后，整个房间被染成灯的绿色/红色 | 复制的默认点光源是"房间照明级"全向光；做指示灯要降到 6% 强度 + 灯球加 emission 自发光 |
| 17 | **无 createLight API** | 想直接创建灯光对象，发现没有对应函数 | CoppeliaSim 4.10 无创建灯光 API；用 `copyPasteObjects` 复制默认灯光 `DefaultLightA`（type=13）+ `setLightParameters(handle, state, None, diffuse, specular)` 设颜色/开关 |

**教训**：CoppeliaSim 4.10 API 签名、坐标系方向、显式触发机制都和直觉/旧资料有出入，**每个 API 先查 `manual\en\regularApi\` 再写，用最小探针脚本实证**（本次用 _probe_vs / _probe_orient 等脚本逐一定位后即删）。

---

## 8. 已知问题与注意事项

1. **Edu 版周期性弹 "Registration" 注册窗**：弹出时 ZMQ 阻塞（脚本无响应）。需手动关闭该弹窗再跑脚本。`saveScene`/`closeScene` 易触发。
2. **乱码残留文件夹** `D:\鍏疯韩鏅鸿兘`（保存中文路径产生，含一份旧场景副本）：沙箱外删不掉，可手动删除。
3. **轮距已按模型实测修正**：`wheel_base = 0.331`（左右轮中心 y 间距 ±0.165，原 0.381 是车身宽，用于运动学是错的）。
4. **相机朝向（v2 已修正）**：视觉传感器默认 +Z 观察，v2 已设 `[0, +π/2, 0]`（绕 Y +90°）使相机看机器人 +X 前进方向。**注意 v1 的 `[0, -π/2, 0]` 实际是看 -X 后方，是错的**。若后续识别时发现方向不对，优先查这里；同时确认 `sim.handleVisionSensor(vis)` 已在抓帧前调用。
5. **尺寸语义**：`createPrimitiveShape` 的 `sizes` 是**全尺寸**（球=直径），非半径/半尺寸。
6. **地板（v4）**：默认 `Floor` 仅 5×5m，机器人驶出边界会坠落。v4 起自建 **12m×6m** 大地板（命名 `/floor`，中心 (2, 0, -0.05)，覆盖 X∈[-4,8] Y∈[-3,3]），清场时删除默认 `Floor`/`box`。机器人落下稳定 z=0.139。
7. **场景丢失风险**：Edu 版弹注册窗、或多次 stopSimulation 后，当前打开的场景可能被清空（实测出现过只剩相机/灯光的空场景）。若 `getObject('/P3DX')` 报「object does not exist」，直接重跑 `python build_scene.py` 重建即可。
8. **指示灯用真实光源（v4）**：指示灯由「可见灯罩球体（亮则 emission 自发光）+ 复制默认点光源（强度 6%）」组成。无 `createLight` API，用 `copyPasteObjects` 复制 `DefaultLightA` + `setLightParameters` 设颜色/开关。**亮灭 = 光源 state=1/0**，不是换颜色。

---

## 9. 下一步计划（M2~M7）

| 里程碑 | 内容 | 关键点 |
|---|---|---|
| M2 | 多点位巡检 + 停靠对准 | 遍历 3 点位，每点转向设备侧停靠 |
| M3 | YOLO 目标识别 + OCR 编号 | 6 类目标 + 柜体编号；训练集用自建场景截图 |
| M4 | 状态判读 | 指示灯颜色/亮灭、开关位置、通道占用（OpenCV 规则为主） |
| M5 | 异常提示 | 仪表超限（Hough 圆+线读指针角度）、红灯告警 |
| M6 | 障碍避让 | LiDAR 绕行 + 接近传感器急停 |
| M7 | 完整结果输出 | JSON/CSV + 现场截图，连续稳定运行 |

> 答辩时间线：线上提交 8/21 12:00–8/25 12:00（报告+代码+视频，最多 5 次）；PPT 同窗口；线下答辩 8/27 下午（杭州电子科技大学，12 分钟）。

---

## 10. 常用操作速查

```bash
cd D:\具身智能\inspection

# 搭建场景
python build_scene.py

# 跑主流程（M1 闭环）
python main.py

# 核对场景对象
python inspect_scene.py

# 复位（停仿真 + 机器人回起点）
python reset_state.py

# 相机对准场景（GUI 直接看）
python view_scene.py
```

**遇到脚本卡住**：先看 CoppeliaSim 有没有弹 "Registration" 窗，有则关掉再重跑。
