# 工业配电房智能巡检赛题——规则文档（沉淀版）

> 赛道：职教赛道（应用与调试）
> 平台：CoppeliaSim（V-REP）
> 定位：重点考核"配置、集成、调试"，不考核算法原创性、自研比例、复杂规划/高精度建图。

---

## 一、核心定位与总原则

1. **应用与调试赛道**：允许使用成熟仿真平台、机器人模型、厂商 SDK、开源软件、预训练模型；不以算法复杂度、模型规模、自研比例作为主要评价依据。
2. **重点评价**：团队自己完成的任务配置、参数设置、功能部署、系统集成、结果设计和系统调试工作。
3. **连续完整运行**：决赛必须在同一场景中**连续**完成"启动→行进→停靠→识别→判读→异常提示→避障→结果输出"，**不得用多个独立功能片段拼接**。
4. **真实性与一致性**：识别结果必须来自**当前仿真运行实时数据**；不得用与当前任务无关的离线图片识别替代；不得预录结果替换；方案报告、代码、演示视频、PPT 必须相互对应。
5. **如实记录**：人工接管、修改位姿、仿真暂停、程序重启、碰撞等情况必须如实记录原因、位置、次数和结果。

---

## 二、仿真场景硬性要求

| 类别 | 最低要求 |
|---|---|
| 巡检点位 | ≥ 2 个（点位、目标类别、正常/异常状态、判断规则须在任务说明中提前明确） |
| 典型目标 | ≥ 2 类（配电柜编号/设备标识、仪表/仪表盘、指示灯、安全警示标识、开关/旋钮部件、通道障碍物等） |
| 异常状态 | ≥ 1 类明确异常（如仪表读数超范围、警示标识缺失、红色告警灯异常亮起等） |
| 障碍物 | ≥ 1 个（检修工具车、绝缘工具箱、检测设备箱、物料箱等） |

**场景特点**：设备排列规则、巡检对象集中、通道边界明确、障碍物可能临时出现。
**允许**：提前建立/导入基础地图、预设巡检点位和主要路线；**不强制**任务运行中自主建图。

---

## 三、七项考察任务与规则

### 1. 仿真环境检查与任务配置
- 检查项：仿真项目加载、机器人模型状态、传感器插件/数据接口、通信与话题连接、场景资源加载确认。
- 配置项：巡检任务选择、运行顺序、点位参数、结果输出方式。
- 启动方式：界面按钮 / 键盘指令 / 上位机指令 / 脚本指令 / 预设任务按钮均可。

### 2. 点位到达与稳定停靠
- 从指定起点出发，**依次到达 ≥2 个规定巡检点位**，在适合观测处**稳定停靠**。
- 可前后微调、转向对准、重新靠近改善停靠位置。
- 不依赖人工持续遥控或人工改位。

### 3. 典型目标识别
- 基于**实时图像/传感器数据**识别 ≥2 类典型目标。
- 可采用规则识别、传统视觉、开源识别模型、预训练模型等。
- 须说明识别对象、配置方法、实际识别效果。

### 4. 目标状态判读
- 在识别基础上对 **≥1 类目标**完成状态判读。
- 示例：指示灯颜色/亮灭、开关旋钮位置、通道畅通/受阻。
- 须明确判读对象、判断规则、参数设置；结果与实际状态基本一致。
- ⚠️ **状态判读与异常提示分开评价，不得重复计分。**

### 5. 异常状态提示与记录
- 针对 **≥1 类明确异常**进行识别、提示、记录。
- 记录内容：异常所在巡检点位、异常对象、异常类型（基本）；检测时间、现场图像等（加分）。
- ⚠️ 一般的指示灯颜色、开关位置、通道状态识别属于**状态判读**，不能作为异常在本项重复计分。

### 6. 障碍感知与安全处置
- 通道中 ≥1 个障碍物；机器人移动中感知障碍物。
- 安全措施：减速、停车等待、绕行、路径调整。
- 无需人工删除障碍物或直接修改位姿帮助通过。

### 7. 巡检结果记录与输出
- 输出方式：仿真界面、终端、网页、语音、表格、文件等。
- 内容须含：任务完成状态、已到达点位、识别目标、状态判读结果、异常提示、任务耗时、碰撞情况、人工接管情况。
- 结果须与演示过程一致，可核验。

---

## 四、评审红线（易丢分项）

1. ❌ 用独立功能片段替代连续完整巡检。
2. ❌ 用离线图片识别结果替代实时仿真识别。
3. ❌ 状态判读与异常提示用同一结果重复计分（须分开设计）。
4. ❌ 任务执行中人工持续遥控 / 人工改位完成点位到达。
5. ❌ 演示视频过度剪辑、预录结果替换。
6. ❌ 现场演示用预录视频替代实际仿真运行。

---

## 五、评分映射（程序作品 100 分 = 10 项 × 10 分）

| # | 评分项 | 分值 | 关键得分点 |
|---|---|---|---|
| 1 | 场景与任务准备 | 10 | 场景契合(2)、≥2点位明确(2)、典型目标明确(2)、状态/异常设置合理(2)、障碍/通道/路线合理(2) |
| 2 | 仿真环境检查与规范操作 | 10 | 项目/场景加载(2)、机器人状态(2)、传感器接口(2)、通信链路(2)、流程清楚(2) |
| 3 | 任务配置与点位设置 | 10 | 任务配置(2)、顺序(2)、点位(2)、参数(2)、结果输出(2) |
| 4 | 点位到达与稳定停靠 | 10 | 第1点位(3)、第2点位(3)、停靠满足观测(2)、合理微调(1)、不遥控(1) |
| 5 | 典型目标识别 | 10 | 第1类(3)、第2类(3)、额外目标(2)、显示清晰(1)、与场景一致(1) |
| 6 | 目标状态判读 | 10 | 完成≥1类(5)、结果一致(2)、对应目标点位(1)、规则参数说明(2) |
| 7 | 异常状态提示与记录 | 10 | 正确识别≥1类(4)、清晰提示(2)、记录点位(1)、记录对象类型(2)、辅助记录(1) |
| 8 | 障碍感知与安全处置 | 10 | 感知障碍(3)、合理措施(3)、无碰撞(2)、继续任务(1)、不需人工帮过(1) |
| 9 | 巡检结果输出与运行稳定性 | 10 | 输出主要结果(4)、一致(2)、清晰可核验(1)、运行稳定(2)、如实记录接管碰撞(1) |
| 10 | 作品完整性与真实性 | 10 | 报告完整(2)、代码对应(2)、视频真实(2)、汇报材料完整(1)、外部来源说明(1)、材料一致(2) |

---

## 六、时间线与提交要求

| 节点 | 时间 | 内容 |
|---|---|---|
| 线上作品提交 | 8月21日 12:00 – 8月25日 12:00 | 方案报告 + 项目代码/运行说明 + 演示视频，打包 zip（最多提交 5 次，取最后一次） |
| 汇报材料提交 | 同上时段 | PPT/PPTX（用主办方指定模板，注意长宽比、页眉页脚、字体字号），打包 zip |
| 线下答辩 | 8月27日（周四）下午 | 杭州电子科技大学，总限时 12 分钟（现场仿真演示 + 技术汇报 + 答辩） |

**zip 命名示例**：
- `参赛作品_teamX_0805.zip`
- `汇报材料_teamX_0805.zip`

**现场演示要求**：
- 自行准备笔记本及演示环境，提前加载场景、检查环境、启动代码、配置参数。
- **使用提交代码现场运行连续巡检任务**，不得用预录视频替代。
- 建议一名主汇报人，其余成员及指导教师候场补答；超时会被主持人直接结束。

---

## 七、提交物清单

### 1. 机器人巡检应用与调试方案报告
仿真平台、机器人模型、仿真场景、系统组成、任务配置、关键参数、目标识别与状态判读方法、异常提示方式、障碍处置、系统调试过程、测试结果、应用价值。

### 2. 项目代码及运行说明
主要代码、配置文件、必要运行说明；外部资源（开源软件、预训练模型、SDK）须说明名称、来源、用途及团队完成的配置/集成/调试工作。

### 3. 机器人巡检任务演示视频
展示：仿真场景、项目启动、任务配置、点位到达停靠、目标识别、状态判读、异常提示、障碍处置、结果输出；保留可判断真实完成的连续过程。

### 4. 汇报答辩 PPT
用指定模板；围绕：背景与任务理解、平台与模型、系统方案、任务配置与实施、主要功能、调试过程、测试结果、典型问题及解决、技术特点、工业应用价值。

---

## 八、答辩评分要点速览（100 分）

| 维度 | 分值 | 要点 |
|---|---|---|
| 任务理解与方案思路 | 20 | 问题/目标、平台选择依据、任务关系、符合应用调试定位 |
| 专业知识与平台应用 | 20 | 平台/模型功能、模块作用、外部工具来源、区分外部功能与自做工作 |
| 配置调试与问题解决 | 25 | 配置过程、识别/判读规则配置、典型问题、定位排查、现场问题处理思路 |
| 任务效果与职业实践 | 15 | 完成情况分析、客观改进方向、工程规范（配置/记录/版本/可追溯） |
| 工业应用与拓展价值 | 10 | 与实际需求对应、效率/安全/管理价值、扩展推广思路 |
| 汇报表达与现场答辩 | 10 | 结构内容、思路时间、回答准确性、成员掌握度 |

---

*本文档为赛题规则沉淀，用于后续方案设计、代码开发、报告撰写与答辩准备的对照依据。*
